<% if @vote.valid? %>
  <% if @comment %>
    alert("commet valid");
  <% end %>
  $('#post_<%= @post.slug %>_votes').html("<%= @post.total_votes %> votes");
<% else %>
  alert("You may only vote on a post once");
<% end %>
